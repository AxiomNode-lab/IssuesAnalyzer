# Web application

Planned responsibility: accessible Next.js user interface and server-side API boundary. No implementation has been selected or generated yet. Browser code must never receive GitHub App private keys, upstream access tokens, database credentials, or raw untrusted HTML.
