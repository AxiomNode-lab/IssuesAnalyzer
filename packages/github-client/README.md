# GitHub client

Adapter for GitHub REST and GraphQL APIs. Responsibilities include schema validation, rate-limit accounting, bounded pagination, retry classification, normalization, and sanitized contract fixtures.
