# Scoring

Pure, deterministic, versioned scoring rules. A scoring result includes component values, weights, evidence references, reasons, confidence, and warnings. Network and database access are prohibited in this package.
