export type CompetitionStatus="none_visible"|"possible"|"visible"|"uncertain";export type ConfidenceLevel="high"|"medium"|"low";export type ActorEvidence=Readonly<{login:string;profileUrl:string}>;export type CommentEvidence=Readonly<{author:ActorEvidence;body:string|null;createdAt:Date;sourceUrl:string;authorIsMaintainer?:boolean}>;export type PullRequestEvidence=Readonly<{number:number;title:string;body:string|null;state:"open"|"closed";draft:boolean;author:ActorEvidence;createdAt:Date;updatedAt:Date;closedAt:Date|null;mergedAt:Date|null;sourceUrl:string}>;export type TimelineEvidence=Readonly<{event:string;actor:ActorEvidence|null;createdAt:Date;sourceUrl:string;referencedPullRequest?:PullRequestEvidence|null}>;export type CompetitionInput=Readonly<{asOf:Date;issue:Readonly<{number:number;canonicalUrl:string;authorLogin:string;assignees:readonly ActorEvidence[]}>;comments:readonly CommentEvidence[]|null;timeline:readonly TimelineEvidence[]|null;pullRequests:readonly PullRequestEvidence[]|null;completeness?:Readonly<{timeline:boolean;pullRequests:boolean}>}>;export type CompetitionFact=Readonly<{key:string;value:string|number|boolean|null;sourceUrl:string;observedAt:Date;freshnessDays:number}>;export type CompetitionInference=Readonly<{key:string;value:string|number|boolean;basisFactKeys:readonly string[];caution:string}>;export type CompetitionResult=Readonly<{version:"competition-v2";status:CompetitionStatus;score:number;confidence:Readonly<{level:ConfidenceLevel;value:number}>;facts:readonly CompetitionFact[];inferences:readonly CompetitionInference[];warnings:readonly string[];evidenceWindow:Readonly<{commentsUsed:number;timelineEventsUsed:number;pullRequestsUsed:number}>}>;
const DAY=86400000,MAX_COMMENTS=500,MAX_TIMELINE_EVENTS=300,MAX_PULL_REQUESTS=100;const CLAIM_PATTERNS=[/\b(?:i(?:'|’)d|i would)\s+(?:like|love|glad|be happy)\s+to\s+(?:work on|take|tackle|handle|pick up|contribute to)\s+(?:this|the)(?:\s+(?:issue|task|one))?\b/i,/\b(?:i(?:'|’)ll|i will|i can|i could|i(?:'|’)m going to|i am going to)\s+(?:work on|take|tackle|handle|pick up)\s+(?:this|the)(?:\s+(?:issue|task|one))?\b/i,/\b(?:can|could|may)\s+i\s+(?:work on|take|tackle|handle|pick up)\s+(?:this|the)(?:\s+(?:issue|task|one))?\b/i,/\b(?:please\s+)?assign\s+(?:this\s+(?:issue|task)\s+to\s+me|me(?:\s+to\s+(?:this|the)(?:\s+(?:issue|task))?)?)\b/i,/\b(?:could|can|would)\s+you\s+(?:please\s+)?assign\s+(?:this\s+(?:issue|task)\s+to\s+)?me\b/i,/\b(?:i(?:'|’)m|i am|currently|already)\s+(?:actively\s+)?working on\s+(?:this|the)(?:\s+(?:issue|task|one))?\b/i,/\b(?:started|starting|began|beginning)\s+(?:to\s+work|working)\s+on\s+(?:this|the)(?:\s+(?:issue|task|one))?\b/i,/\b(?:working on|taking|tackling|handling|picking up)\s+(?:this|the)\s+(?:issue|task|one)\b/i,/\blet\s+me\s+(?:work on|take|tackle|handle|pick up)\s+(?:this|the)(?:\s+(?:issue|task|one))?\b/i,/\bclaim(?:ing)?\s+(?:this|the)\s+(?:issue|task|one)\b/i,/\b(?:i(?:'|’)d|i would|i(?:'|’)ll|i will|i can)\s+(?:love|like|be happy|be glad)?\s*(?:to\s+)?take\s+(?:this|the)\s+one\s+on\b/i,/\b(?:has|have|had|i(?:'|’)ve|i have)\s+applied\s+to\s+work\s+on\s+(?:this|the)\s+(?:issue|task)\b/i,/\bapplied\s+for\s+(?:this|the)\s+(?:issue|task)\b/i,/\bapplication\s+to\s+work\s+on\s+(?:this|the)\s+(?:issue|task)\b/i,/\b(?:ready|available)\s+to\s+(?:start|get started|work)\s+on\s+(?:this|the)\s+(?:issue|task)\b/i,/\b(?:i(?:'|’)m|i am)\s+(?:ready|available)\s+to\s+get\s+started\s+(?:on\s+(?:this|the)\s+(?:issue|task))?\b/i,/\b(?:i(?:'|’)ll|i will)\s+(?:get started|start working)\s+(?:on\s+)?(?:this|the)\s+(?:issue|task)\b/i,/\b(?:i(?:'|’)d|i would)\s+like\s+to\s+contribute\s+(?:to|on)\s+(?:this|the)\s+(?:issue|task)\b/i];const NON_CLAIM_PATTERNS=[/\b(?:i(?:'|’)m not|i am not|not)\s+working on\s+(?:this|the)(?:\s+(?:issue|task|one))?\b/i,/\b(?:i can(?:'|’)t|i cannot|i won(?:'|’)t|i will not)\s+(?:work on|take|tackle|handle)\s+(?:this|the)(?:\s+(?:issue|task|one))?\b/i,/\b(?:no longer|stopped|stop|dropping|drop|giving up|gave up)\s+(?:working on|work on|this|the)\b/i,/\b(?:please\s+)?unassign\s+me\b/i];const hasClaimLanguage=(b:string)=>!NON_CLAIM_PATTERNS.some(p=>p.test(b))&&CLAIM_PATTERNS.some(p=>p.test(b));function validDate(v:Date,n:string){if(Number.isNaN(v.getTime()))throw new TypeError(`Invalid ${n} date.`)}function historicalDate(v:Date,a:Date,n:string){validDate(v,n);if(v>a)throw new RangeError(`${n} date cannot be after asOf.`)}function daysBetween(l:Date,e:Date){return Math.max(0,Math.floor((l.getTime()-e.getTime())/DAY))}function newestBounded<T>(items:readonly T[],limit:number,dateOf:(i:T)=>Date,keyOf:(i:T)=>string){return [...items].sort((a,b)=>dateOf(b).getTime()-dateOf(a).getTime()||keyOf(a).localeCompare(keyOf(b))).slice(0,limit)}const isBot=(l:string)=>l.toLowerCase().endsWith("[bot]");function referencesIssue(t:string,n:number,u:string){if(t.includes(u))return true;return new RegExp(`(^|\\s)#${n}(?=$|[\\s.,;:!?)}\\]])`).test(t)}function referencedPrNumbers(t:string){return [...new Set([...t.matchAll(/#(\d+)\b/g)].map(m=>Number(m[1])).filter(Number.isSafeInteger))]}const RESOLUTION_LANGUAGE=/\b(?:fix(?:ed|es)?|resolv(?:e|ed|es)|implement(?:ed|s)?|address(?:ed|es)?|work(?:ing)?\s+on|handled?)\b/i;function confidence(s:number,i:number){const value=Math.max(0,Math.round(s/4*100)-i*30);return{level:value>=75?"high" as const:value>=50?"medium" as const:"low" as const,value}}function validatePullRequest(p:PullRequestEvidence,a:Date){historicalDate(p.createdAt,a,"pull request creation");historicalDate(p.updatedAt,a,"pull request update");if(p.closedAt)historicalDate(p.closedAt,a,"pull request closure");if(p.mergedAt)historicalDate(p.mergedAt,a,"pull request merge")}function claimRisk(c:number,d:number){if(!c)return 0;const base=c>=8?80:c>=6?70:c>=4?60:c>=2?35:15,f=d<=14?5:d<=45?0:-5;return Math.max(10,Math.min(90,base+f))}
export function analyzeCompetition(input:CompetitionInput):CompetitionResult{validDate(input.asOf,"asOf");if(!Number.isSafeInteger(input.issue.number)||input.issue.number<=0)throw new RangeError("Issue number must be a positive safe integer.");for(const i of input.comments??[])historicalDate(i.createdAt,input.asOf,"comment");for(const i of input.timeline??[]){historicalDate(i.createdAt,input.asOf,"timeline event");if(i.referencedPullRequest)validatePullRequest(i.referencedPullRequest,input.asOf)}for(const i of input.pullRequests??[])validatePullRequest(i,input.asOf);const comments=newestBounded(input.comments??[],MAX_COMMENTS,i=>i.createdAt,i=>i.sourceUrl),timeline=newestBounded(input.timeline??[],MAX_TIMELINE_EVENTS,i=>i.createdAt,i=>`${i.event}:${i.sourceUrl}`),prs=newestBounded(input.pullRequests??[],MAX_PULL_REQUESTS,i=>i.updatedAt,i=>i.sourceUrl),claims=comments.filter(c=>c.body!==null&&c.author.login.toLowerCase()!==input.issue.authorLogin.toLowerCase()&&!isBot(c.author.login)&&hasClaimLanguage(c.body));const explicit=prs.filter(p=>referencesIssue(`${p.title}\n${p.body??""}`,input.issue.number,input.issue.canonicalUrl)),timelinePrs=timeline.flatMap(e=>e.referencedPullRequest?[e.referencedPullRequest]:[]);const mentioned=new Set(comments.filter(c=>c.body&&RESOLUTION_LANGUAGE.test(c.body)&&!isBot(c.author.login)).flatMap(c=>referencedPrNumbers(c.body!))),commentLinked=prs.filter(p=>mentioned.has(p.number));const byUrl=new Map<string,PullRequestEvidence>();for(const p of [...explicit,...timelinePrs,...commentLinked])byUrl.set(p.sourceUrl,p);const linked=[...byUrl.values()].sort((a,b)=>b.updatedAt.getTime()-a.updatedAt.getTime()),active=linked.filter(p=>p.state==="open"),draft=active.filter(p=>p.draft),merged=linked.filter(p=>p.mergedAt!==null),closed=linked.filter(p=>p.state==="closed"&&p.mergedAt===null),nonPr=timeline.filter(e=>["cross-referenced","connected","referenced"].includes(e.event)&&!e.referencedPullRequest),freshest=linked[0],latestClaim=claims[0];const fact=(key:string,value:string|number|boolean|null,url:string,d:number):CompetitionFact=>({key,value,sourceUrl:url,observedAt:input.asOf,freshnessDays:d});const facts:CompetitionFact[]=[fact("issue.assigneeCount",input.issue.assignees.length,input.issue.canonicalUrl,0),fact("competition.linkedPullRequestCount",linked.length,freshest?.sourceUrl??input.issue.canonicalUrl,freshest?daysBetween(input.asOf,freshest.updatedAt):0),fact("competition.activePullRequestCount",active.length,active[0]?.sourceUrl??input.issue.canonicalUrl,active[0]?daysBetween(input.asOf,active[0].updatedAt):0),fact("competition.draftPullRequestCount",draft.length,draft[0]?.sourceUrl??input.issue.canonicalUrl,draft[0]?daysBetween(input.asOf,draft[0].updatedAt):0),fact("competition.mergedPullRequestCount",merged.length,merged[0]?.sourceUrl??input.issue.canonicalUrl,merged[0]?.mergedAt?daysBetween(input.asOf,merged[0].mergedAt):0),fact("competition.closedPullRequestCount",closed.length,closed[0]?.sourceUrl??input.issue.canonicalUrl,closed[0]?.closedAt?daysBetween(input.asOf,closed[0].closedAt):0),fact("competition.claimCommentCount",claims.length,latestClaim?.sourceUrl??input.issue.canonicalUrl,latestClaim?daysBetween(input.asOf,latestClaim.createdAt):0),fact("competition.nonPullRequestReferenceCount",nonPr.length,input.issue.canonicalUrl,0)];if(active.length)facts.push(fact("competition.activeImplementation",true,active[0]!.sourceUrl,daysBetween(input.asOf,active[0]!.updatedAt)));if(input.issue.assignees.length)facts.push(fact("competition.assignedIssue",true,input.issue.canonicalUrl,0));if(claims.length>=6)facts.push(fact("competition.crowdedClaims",true,latestClaim?.sourceUrl??input.issue.canonicalUrl,latestClaim?daysBetween(input.asOf,latestClaim.createdAt):0));const inferences:CompetitionInference[]=[];if(input.issue.assignees.length)inferences.push({key:"competition.assigned",value:true,basisFactKeys:["issue.assigneeCount"],caution:"Assignment is visible evidence, but it does not prove work is still active."});if(active.length)inferences.push({key:"competition.activeImplementation",value:true,basisFactKeys:["competition.activePullRequestCount"],caution:"An open or draft linked pull request is active visible work, but it may still be abandoned."});if(claims.length)inferences.push({key:"competition.claimLanguage",value:true,basisFactKeys:["competition.claimCommentCount"],caution:"Claim language indicates intent only; it is weighted below assignment or an active pull request."});if(!input.issue.assignees.length&&!active.length&&!claims.length)inferences.push({key:"competition.noneActiveVisible",value:true,basisFactKeys:["issue.assigneeCount","competition.activePullRequestCount","competition.claimCommentCount"],caution:"No active visible signal does not guarantee nobody is working privately."});const completeness=input.completeness??{timeline:true,pullRequests:true},warnings:string[]=[];if(input.comments===null)warnings.push("Comment evidence is unavailable.");if(input.timeline===null)warnings.push("Timeline evidence is unavailable.");if(input.pullRequests===null)warnings.push("Pull request evidence is unavailable.");if(!completeness.timeline)warnings.push("Timeline evidence reached its collection bound; additional linked work may exist.");if(!completeness.pullRequests)warnings.push("Repository pull request evidence reached its collection bound.");if((input.comments?.length??0)>MAX_COMMENTS)warnings.push("Comment evidence was bounded to 500 records.");if((input.timeline?.length??0)>MAX_TIMELINE_EVENTS)warnings.push("Timeline evidence was bounded to 300 records.");if((input.pullRequests?.length??0)>MAX_PULL_REQUESTS)warnings.push("Pull request evidence was bounded to 100 records.");const conf=confidence(1+Number(input.comments!==null)+Number(input.timeline!==null)+Number(input.pullRequests!==null),Number(input.timeline!==null&&!completeness.timeline)+Number(input.pullRequests!==null&&!completeness.pullRequests)),days=latestClaim?daysBetween(input.asOf,latestClaim.createdAt):0,score=Math.max(active.length>=3?100:active.length===2?90:active.length===1?80:0,input.issue.assignees.length?75:0,claimRisk(claims.length,days),merged.length?20:0,closed.length?10:0),status:CompetitionStatus=active.length||input.issue.assignees.length?"visible":claims.length||merged.length+closed.length?"possible":input.comments===null&&input.timeline===null&&input.pullRequests===null?"uncertain":"none_visible";return{version:"competition-v2",status,score,confidence:conf,facts,inferences,warnings,evidenceWindow:{commentsUsed:comments.length,timelineEventsUsed:timeline.length,pullRequestsUsed:prs.length}}}
